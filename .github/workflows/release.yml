name: release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: verify package.json version matches tag
        run: |
          TAG="${{ github.ref_name }}"
          PKG_VERSION="v$(node -p "require('./package.json').version")"
          if [[ "$TAG" != "$PKG_VERSION" ]]; then
            echo "error: tag $TAG does not match package.json version $PKG_VERSION"
            echo "Run 'bun run release' locally instead of tagging manually."
            exit 1
          fi

      - name: build sqlite-admin-src.zip
        run: |
          zip -rq sqlite-admin-src.zip src server.js package.json README.md \
            -x "node_modules/*" ".git/*" "**/.DS_Store"

      - name: create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            sqlite-admin-src.zip
